# Copy to .env.development (gitignored). Run the server from this directory.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/library_dev?sslmode=disable

# Optional; defaults to :3000 when unset.
# SERVER_ADDR=:3000
